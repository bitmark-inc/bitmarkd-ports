#!/bin/sh

case "${2}" in
  (PRE-INSTALL)
    true
    ;;

  (POST-INSTALL)
    if [ -d "%%SCRIPTDIR%%" ]
    then
      chown '%%USERS%%:%%GROUPS%%' "%%SCRIPTDIR%%"
      cd "%%SCRIPTDIR%%" || exit 1
      su -m '%%USERS%%' -c 'python2.7 compile.py'
    fi
    ;;

esac
