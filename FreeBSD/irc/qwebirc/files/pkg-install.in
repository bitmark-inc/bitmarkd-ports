#!/bin/sh

case "${2}" in
  (PRE-INSTALL)
    true
    ;;

  (POST-INSTALL)
    if [ -d "%%PREFIX%%/www/${1}" ]
    then
      cd "%%PREFIX%%/www/${1}" || exit 1
      python2.7 compile.py
    fi
    ;;

esac
