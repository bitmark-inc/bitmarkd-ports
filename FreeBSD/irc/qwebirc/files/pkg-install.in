#!/bin/sh

case "${2}" in
  (PRE-INSTALL)
    true
    ;;

  (POST-INSTALL)
    if [ -d "%%PREFIX%%/www/${1}" ]
    then
      cd "%%PREFIX%%/www/${1}" || exit 1
      chown '%%USERS%%:GROUPS%%' "%%PREFIX%%/www/${1}"
      su -m '%%USERS%%' -c 'python2.7 compile.py'
    fi
    ;;

esac
